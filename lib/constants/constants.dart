import 'dart:ui';

class Constants {
  static const appTitle = 'Coin Flip';
  static const settingsBox = 'settings';
  static const assetsImagesPath = 'assets/images/';
  static const defaultLocale = Locale('en');
  static const supportedLocales = [
    Locale('en'),
    Locale('de'),
    Locale('ru'),
    Locale('kg'),
    Locale('tr')
  ];
}
